so the plane is that to make a screen recoder  for pc (free) --opensource project

in this software should be having the following features 

1) customise scree selector(sound)
2) sound recorder(free noise removal and auto silence removal )
3) cam recorder
4) screen + cam recorder(dual mode)(free noise removal and auto silence removal )
  in dual mode the webcame show in side of window and it will be moveable and the size also changeble like circle,rectangle

